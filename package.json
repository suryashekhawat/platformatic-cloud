{
  "scripts": {
    "start": "npm run clean && platformatic db start",
    "clean": "rm -fr ./dist",
    "build": "npx tsc"
  },
  "devDependencies": {
    "fastify": "^4.14.1"
  },
  "dependencies": {
    "platformatic": "^0.18.0"
  },
  "engines": {
    "node": "^16.17.0 || ^18.8.0 || >=19"
  }
}